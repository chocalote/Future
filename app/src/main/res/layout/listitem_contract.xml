<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:orientation="horizontal"
    android:weightSum="1">

    <RelativeLayout
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:layout_weight="0.5"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/contractSubName"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginLeft="20dp"
            android:gravity="center_vertical" />

        <TextView
            android:id="@+id/contractName"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignBaseline="@id/contractSubName"
            android:layout_toEndOf="@id/contractSubName"
            android:textColor="@color/colorPrimaryDark"
            android:textSize="12sp" />
    </RelativeLayout>

    <TextView
        android:id="@+id/contractLatestPrice"
        style="@style/contractStyle"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:layout_weight="0.3" />

    <TextView
        android:id="@+id/contractChange"
        style="@style/contractStyle"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:layout_weight="0.2" />

</LinearLayout>